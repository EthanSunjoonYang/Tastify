server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Client-side routing (react-router) -- unmatched paths fall back to
    # index.html so /lobby/<id> etc. don't 404 on a hard refresh.
    location / {
        try_files $uri /index.html;
    }
}
